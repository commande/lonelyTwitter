diff --git a/res/layout/main.xml b/res/layout/main.xml
index c2eec36..78f66ef 100644
--- a/res/layout/main.xml
+++ b/res/layout/main.xml
@@ -41,6 +41,7 @@
         android:id="@+id/save"
         android:layout_width="fill_parent"
         android:layout_height="wrap_content"
-        android:text="@string/save" />
+        android:text="@string/save" 
+        android:onClick="onClickSave"/>
 
 </LinearLayout>
\ No newline at end of file
diff --git a/src/ca/ualberta/cs/lonelytwitter/CustomImportantTweetItem.java b/src/ca/ualberta/cs/lonelytwitter/CustomImportantTweetItem.java
new file mode 100644
index 0000000..17c9b4e
--- /dev/null
+++ b/src/ca/ualberta/cs/lonelytwitter/CustomImportantTweetItem.java
@@ -0,0 +1,17 @@
+package ca.ualberta.cs.lonelytwitter;
+
+public class CustomImportantTweetItem extends CustomTweetItem {
+	
+	private String prefix = "Important Tweet: ";
+
+	public CustomImportantTweetItem(String text, String author) {
+		super(text, author);
+	}
+	
+	@Override
+	public String toString() {
+		return this.prefix + this.getText();
+	}
+
+
+}
diff --git a/src/ca/ualberta/cs/lonelytwitter/CustomNormalTweetItem.java b/src/ca/ualberta/cs/lonelytwitter/CustomNormalTweetItem.java
new file mode 100644
index 0000000..66df474
--- /dev/null
+++ b/src/ca/ualberta/cs/lonelytwitter/CustomNormalTweetItem.java
@@ -0,0 +1,17 @@
+package ca.ualberta.cs.lonelytwitter;
+
+public class CustomNormalTweetItem extends CustomTweetItem {
+	
+	private String prefix = "Normal Tweet: ";
+
+	public CustomNormalTweetItem(String text, String author) {
+		super(text, author);
+	}
+	
+	@Override
+	public String toString() {
+		// TODO Auto-generated method stub
+		return this.prefix + this.getText();
+	}
+
+}
diff --git a/src/ca/ualberta/cs/lonelytwitter/CustomTweetInterface.java b/src/ca/ualberta/cs/lonelytwitter/CustomTweetInterface.java
new file mode 100644
index 0000000..b4c53e4
--- /dev/null
+++ b/src/ca/ualberta/cs/lonelytwitter/CustomTweetInterface.java
@@ -0,0 +1,9 @@
+package ca.ualberta.cs.lonelytwitter;
+
+import java.util.Date;
+
+public interface CustomTweetInterface {
+	String getText();
+	String getAuthor();
+	Date getCreationDate();
+}
diff --git a/src/ca/ualberta/cs/lonelytwitter/CustomTweetItem.java b/src/ca/ualberta/cs/lonelytwitter/CustomTweetItem.java
new file mode 100644
index 0000000..34c404f
--- /dev/null
+++ b/src/ca/ualberta/cs/lonelytwitter/CustomTweetItem.java
@@ -0,0 +1,31 @@
+package ca.ualberta.cs.lonelytwitter;
+
+import java.util.Date;
+
+public abstract class CustomTweetItem implements CustomTweetInterface {
+	
+	private String text;
+	private String author;
+	private Date creationDate;
+	
+	public CustomTweetItem(String text, String author) {
+		this.text = text;
+		this.author = author;
+		this.creationDate = new Date();
+	}
+
+	public String getText() {
+		return this.text;
+	}
+
+	public String getAuthor() {
+		return this.author;
+	}
+
+	public Date getCreationDate() {
+		return this.creationDate;
+	}
+	
+	public abstract String toString();
+
+}
diff --git a/src/ca/ualberta/cs/lonelytwitter/Interface1.java b/src/ca/ualberta/cs/lonelytwitter/Interface1.java
new file mode 100644
index 0000000..5ded6d9
--- /dev/null
+++ b/src/ca/ualberta/cs/lonelytwitter/Interface1.java
@@ -0,0 +1,6 @@
+package ca.ualberta.cs.lonelytwitter;
+
+public interface Interface1 {
+	String s = "This is interface";
+	String getText();
+}
diff --git a/src/ca/ualberta/cs/lonelytwitter/InterfaceImp1.java b/src/ca/ualberta/cs/lonelytwitter/InterfaceImp1.java
new file mode 100644
index 0000000..931b812
--- /dev/null
+++ b/src/ca/ualberta/cs/lonelytwitter/InterfaceImp1.java
@@ -0,0 +1,9 @@
+package ca.ualberta.cs.lonelytwitter;
+
+public class InterfaceImp1 implements Interface1 {
+
+	public String getText() {
+		return s + " implementation";
+	}
+
+}
diff --git a/src/ca/ualberta/cs/lonelytwitter/LonelyTwitterActivity.java b/src/ca/ualberta/cs/lonelytwitter/LonelyTwitterActivity.java
index cd5feb6..0f39db9 100644
--- a/src/ca/ualberta/cs/lonelytwitter/LonelyTwitterActivity.java
+++ b/src/ca/ualberta/cs/lonelytwitter/LonelyTwitterActivity.java
@@ -8,10 +8,12 @@ import java.io.IOException;
 import java.io.InputStreamReader;
 import java.util.ArrayList;
 import java.util.Date;
+import java.util.HashSet;
 
 import android.app.Activity;
 import android.content.Context;
 import android.os.Bundle;
+import android.util.Log;
 import android.view.View;
 import android.widget.ArrayAdapter;
 import android.widget.Button;
@@ -23,6 +25,7 @@ public class LonelyTwitterActivity extends Activity {
 	private static final String FILENAME = "file.sav";
 	private EditText bodyText;
 	private ListView oldTweetsList;
+	HashSet<String> h;
 	
 	/** Called when the activity is first created. */
 	@Override
@@ -34,16 +37,17 @@ public class LonelyTwitterActivity extends Activity {
 		Button saveButton = (Button) findViewById(R.id.save);
 		oldTweetsList = (ListView) findViewById(R.id.oldTweetsList);
 
-		saveButton.setOnClickListener(new View.OnClickListener() {
-
-			public void onClick(View v) {
-				setResult(RESULT_OK);
-				String text = bodyText.getText().toString();
-				saveInFile(text, new Date(System.currentTimeMillis()));
-				finish();
-
-			}
-		});
+//		saveButton.setOnClickListener(new View.OnClickListener() {
+//
+//			
+//		});
+	}
+	
+	public void onClickSave(View v) {
+		setResult(RESULT_OK);
+		String text = bodyText.getText().toString();
+		saveInFile(text, new Date(System.currentTimeMillis()));
+		finish();
 	}
 
 	@Override
@@ -66,7 +70,27 @@ public class LonelyTwitterActivity extends Activity {
 				tweets.add(line);
 				line = in.readLine();
 			}
-
+			
+			/***
+			 * ADD CUSTOM CODE
+			 */
+			
+//			Test t = new Test("Hello");
+//			// t.setText("abc");
+//			tweets.add(t.getText());
+//			
+//			Test t2 = new Test2("Hi");
+//			tweets.add(t2.getText());
+			
+//			InterfaceImp1 ii = new InterfaceImp1();
+//			tweets.add(ii.getText());
+			
+			CustomNormalTweetItem normalTweet = new CustomNormalTweetItem("Hello", "Brett");
+			Log.i("CUSTOM_TWEET", normalTweet.toString());
+			
+			CustomImportantTweetItem importantTweet = new CustomImportantTweetItem("OMG!!!", "Brett");
+			Log.i("CUSTOM_TWEET", importantTweet.toString());
+			
 		} catch (FileNotFoundException e) {
 			// TODO Auto-generated catch block
 			e.printStackTrace();
@@ -81,7 +105,7 @@ public class LonelyTwitterActivity extends Activity {
 		try {
 			FileOutputStream fos = openFileOutput(FILENAME,
 					Context.MODE_APPEND);
-			fos.write(new String(date.toString() + " | " + text)
+			fos.write(new String(date.toString() + " | " + text + "\n")
 					.getBytes());
 			fos.close();
 		} catch (FileNotFoundException e) {
diff --git a/src/ca/ualberta/cs/lonelytwitter/Test.java b/src/ca/ualberta/cs/lonelytwitter/Test.java
new file mode 100644
index 0000000..9103d23
--- /dev/null
+++ b/src/ca/ualberta/cs/lonelytwitter/Test.java
@@ -0,0 +1,14 @@
+package ca.ualberta.cs.lonelytwitter;
+
+public class Test {
+	private String text;
+	public Test(String text) {
+		this.text = text;
+	}
+	public String getText() {
+		return this.text;
+	}
+	public void setText(String t) {
+		text = t;
+	}
+}
diff --git a/src/ca/ualberta/cs/lonelytwitter/Test2.java b/src/ca/ualberta/cs/lonelytwitter/Test2.java
new file mode 100644
index 0000000..120e1d3
--- /dev/null
+++ b/src/ca/ualberta/cs/lonelytwitter/Test2.java
@@ -0,0 +1,10 @@
+package ca.ualberta.cs.lonelytwitter;
+
+public class Test2 extends Test {
+
+	public Test2(String text) {
+		super(text);
+		this.setText("Hello " + text);
+	}
+
+}
